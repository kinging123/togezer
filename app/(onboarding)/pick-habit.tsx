import { SafeAreaView, Text } from 'react-native'

export default function PickHabitScreen() {
  return (
    <SafeAreaView style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>pick habit — coming soon</Text>
    </SafeAreaView>
  )
}
