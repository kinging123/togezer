import { SafeAreaView, Text } from 'react-native'
export default function TodayScreen() {
  return <SafeAreaView style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}><Text>today</Text></SafeAreaView>
}
